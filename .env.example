# Copy to `.env` and fill in. Never commit the real .env (it's gitignored).

# ── Where CTC is served (the one host knob) ──────────────────────────────────
# Sets the web app + API origin AND drives the CLI install command, the proxy
# host shown in "Set up CLI", and the launcher's default host — all derived from
# this single value. Use a hostname OR a raw IP:
#   CTC_DOMAIN=ctc.company.com   → public/internal DNS name (Let's Encrypt if public)
#   CTC_DOMAIN=10.0.0.5          → raw internal IP (Caddy serves HTTPS via its
#                                   internal CA; teammates trust that CA once —
#                                   see docs/guide/07-deploying.md)
#   CTC_DOMAIN=localhost         → local test (Caddy internal CA)
CTC_DOMAIN=ctc.example.com

# ── Ports (optional) ─────────────────────────────────────────────────────────
# Each service has TWO ports that only differ if a front server forwards to this
# host on a different port (e.g. a public box Y maps its :80 → this box's :5050):
#   • the PUBLIC port teammates actually reach (baked into the app + CLI URLs)
#   • the port the container BINDS here (what the forward targets)
# If you serve this host directly (no forward), set them to the same value.
# After any change, rebuild — the URLs are compiled in: docker compose up -d --build
#
#   CTC_DOMAIN            → PUBLIC host teammates reach (the front server's name/IP)
#   CTC_WEB_PORT_SUFFIX   → PUBLIC web port baked into URLs. Blank = standard 80/443;
#                           otherwise INCLUDE THE COLON, e.g. ":8443" or ":5000".
#   CTC_HTTP_PORT         → web port this container binds in HTTP mode  (forward target)
#   CTC_HTTPS_PORT        → web port this container binds in HTTPS mode (forward target)
#   CTC_PROXY_PORT        → proxy port this container binds/publishes   (forward target)
#   CTC_PROXY_PUBLIC_PORT → PUBLIC proxy port the CLI connects to
#   CTC_PROXY_HOST        → host the CLI reaches the proxy at. Blank = same as the
#                           web host (CTC_DOMAIN). Set this to keep the proxy on a
#                           private/VPN address while the website is public — e.g.
#                           CTC_DOMAIN=protoops.example.com (public web) +
#                           CTC_PROXY_HOST=10.0.0.5 (VPN-only proxy).
# (Kept as plain ${VAR:-default} interpolation so BOTH docker-compose v1 and the
#  v2 `docker compose` plugin accept this file.)
#
# Example — public box Y forwards Y:80→X:5050 (web) and Y:8080→X:9090 (proxy),
# with CTC_WEB_TRANSPORT=http:
#   CTC_DOMAIN=y.public.example.com
#   CTC_WEB_PORT_SUFFIX=     # teammates hit :80 → leave blank
#   CTC_HTTP_PORT=5050       # this container binds 5050; Y forwards 80→5050
#   CTC_PROXY_PORT=9090      # this container binds 9090; Y forwards 8080→9090
#   CTC_PROXY_PUBLIC_PORT=8080   # CLI connects to y.public…:8080
# Direct (no front server) example — serve HTTPS on 8443:
#   CTC_WEB_PORT_SUFFIX=:8443   and   CTC_HTTPS_PORT=8443
# Also update your OAuth app's callback URL to the public origin (host + port).
CTC_WEB_PORT_SUFFIX=
CTC_HTTP_PORT=80
CTC_HTTPS_PORT=443
CTC_PROXY_PORT=8080
CTC_PROXY_PUBLIC_PORT=8080
CTC_PROXY_HOST=

# ── Secrets ──────────────────────────────────────────────────────────────────
# One key, shared by the proxy + control plane. It encrypts stored PATs and
# signs session/state cookies. Generate once, keep secret, never change it
# (changing it makes stored PATs undecryptable). e.g.  openssl rand -hex 32
CTC_SECRET_KEY=change-me-to-a-long-random-secret

# ── Deployment mode (shipped defaults target license-holders-trade-credits) ──
# Website transport. http = plain HTTP (NO TLS) — ONLY behind a VPN/trusted LAN.
# Must match CTC_APP_ORIGIN scheme. For public TLS use https + CADDYFILE=Caddyfile.
CTC_WEB_TRANSPORT=http
# Caddy config file to mount. Default (unset) = Caddyfile (HTTPS with TLS).
# Set to Caddyfile.http for plain HTTP mode (CTC_WEB_TRANSPORT=http).
CADDYFILE=Caddyfile.http
# Live admin toggles seed their FIRST-BOOT default from these; afterwards the
# admin panel value wins.
CTC_PARTICIPANTS_MODE=givers_only        # or givers_and_consumers
CTC_SHARED_POOL=off                      # or on

# ── GitLab OAuth app ─────────────────────────────────────────────────────────
# GitLab is the sole auth path. Register an OAuth application in GitLab:
#   GitLab → User Settings → Applications → New application
#   Scopes: read_user only. Confidential: yes.
#   Redirect URI: must exactly equal GITLAB_OAUTH_REDIRECT_URI below.
#   The redirect URI must be localhost or HTTPS (GitLab rejects plain-http non-localhost).
GITLAB_BASE=https://gitlab.company.com
GITLAB_OAUTH_CLIENT_ID=
GITLAB_OAUTH_CLIENT_SECRET=
GITLAB_OAUTH_REDIRECT_URI=http://localhost:8090/auth/callback

# Your GitHub Enterprise domain. The proxy derives the hosts it decrypts/swaps
# from this (contract.py), the cert SANs come from it (gen-cert.sh), and the CLI
# launcher uses it for GH_HOST. Set it once here to your real GHE domain.
GHE_DOMAIN=example.ghe.com

# GHE API host that serves /copilot_internal/user (Copilot quota / PAT validation).
# REQUIRED — PAT validation always uses the API host.
GHE_API_BASE=https://api.example.ghe.com

# The real GHE API host the proxy forwards Copilot traffic to.
REAL_GHE_HOST=api.example.ghe.com

# ── Proxy network exposure ───────────────────────────────────────────────────
# The proxy is an HTTP CONNECT proxy. Two exposure models:
#
#   VPN/internal only (default): set PROXY_BIND to the VM's VPN-facing IP so only
#   teammates on the VPN reach it; leave CTC_RESTRICT_CONNECT off. 127.0.0.1 keeps
#   it localhost-only.
#
#   Public via a front server: only safe if the front raw-forwards a dedicated
#   LAYER-4 (TCP passthrough) port to the proxy — an HTTP/L7 reverse proxy REJECTS
#   the CONNECT tunnel method (returns 503), so the CLI can't traverse it. When the
#   proxy is thus publicly reachable, set CTC_RESTRICT_CONNECT=1 to refuse tunnels
#   to anything outside the GitHub/GHE/Copilot host set (kills the open-relay risk).
#   Point the CLI at it with CTC_PROXY_HOST=<front host> + CTC_PROXY_PUBLIC_PORT=<L4 port>.
PROXY_BIND=127.0.0.1
CTC_RESTRICT_CONNECT=

# ── Admin panel ──────────────────────────────────────────────────────────────
# Comma-separated identities that get the admin panel (see all users, reveal
# PATs, edit global defaults). Case-insensitive. Lock this down like a secret —
# an admin can reveal every giver's real PAT. Empty/unset = nobody is admin.
# List GitLab USERNAMES (the ghe_login field stores the GitLab username).
CTC_ADMINS=your-gitlab-username

# ── Defaults ─────────────────────────────────────────────────────────────────
# % of a giver's REMAINING quota auto-pledged to the shared pool when they submit
# their PAT (they can change it afterwards in Settings). Default 10.
CTC_DEFAULT_PLEDGE_PCT=10
